@model FrontEnd.Models.UsuarioViewModel

@{
    ViewData["Title"] = "Edit";
}

<link rel="stylesheet" href="~/css/Pedidos.css" />

<div class="edit-container">
    <h1 class="page-title">Editar Usuario</h1>
    <h4>Detalles del Usuario</h4>
    <hr />

    <div class="form-container">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="form-group">
                <label asp-for="IdUsuario" class="form-label"></label>
                <input asp-for="IdUsuario" class="form-input" readonly />
                <span asp-validation-for="IdUsuario" class="error-message"></span>
            </div>

            <div class="form-group">
                <label asp-for="Nombre" class="form-label"></label>
                <input asp-for="Nombre" class="form-input" />
                <span asp-validation-for="Nombre" class="error-message"></span>
            </div>

            <div class="form-group">
                <label asp-for="Correo" class="form-label"></label>
                <input asp-for="Correo" class="form-input" />
                <span asp-validation-for="Correo" class="error-message"></span>
            </div>

            <div class="form-group">
                @Html.HiddenFor(model => model.IdUsuario)
                <input type="submit" value="Guardar Cambios" class="btn btn-save" />
            </div>
        </form>
    </div>

    <div class="back-link">
        <a asp-action="Index" class="btn btn-back">Volver a la Lista</a>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

